TRANSCRIBE_PROVIDER=local
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

SUMMARY_PROVIDER=openai
SUMMARY_MODEL=gpt-4o-mini

OPENAI_API_KEY=
OPENAI_BASE_URL=

MAX_UPLOAD_MB=200
DEFAULT_LANGUAGE=th

# Admin password — ถ้าตั้งค่า จะต้องใส่ใน Settings UI เพื่อแก้ config (เว้นว่าง = เปิดให้ทุกคน)
ADMIN_PASSWORD=
